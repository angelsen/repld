---
title: Help topics
description: API reference for exec, defer, every, channels, browser, and gists.
---

> Placeholder — this section will be generated at build time from
> `src/repld/help.py` (`_TOPICS` + `GUIDE`) so the site can never drift from
> what the agent and `repld help` see. See `site/scripts/gen-reference.py`.

Until then, run `repld help <topic>` locally: `exec`, `browser`, `gists`.
