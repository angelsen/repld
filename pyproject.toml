[project]
name = "repld-tool"
version = "0.2.4"
description = "Persistent Python runtime with MCP channel push. Dev shell and autonomous-agent substrate in one package."
readme = "README.md"
authors = [
    { name = "Fredrik Angelsen", email = "fredrikangelsen@gmail.com" }
]
requires-python = ">=3.12"
license = "MIT"
license-files = ["LICENSE"]
classifiers = [
    "Development Status :: 3 - Alpha",
    "Programming Language :: Python :: 3",
]
keywords = ["repl", "mcp", "agent", "browser"]
dependencies = []

[project.scripts]
repld = "repld:main"

[project.optional-dependencies]
pretty = [
    "rich>=15.0.0",
]
browser = [
    "duckdb>=1.5.2",
    "websockets>=16.0",
    "pillow>=11.0",
]

[dependency-groups]
# Optional extras installed in dev so basedpyright can resolve their imports.
# httpx is not repld's at all — five gists in ./gists declare it via
# __repld_deps__, and a kernel installs it into the shared gist-deps dir at
# runtime. basedpyright can't see that directory (it's keyed by interpreter
# version and lives outside the project), so without this `uv sync` prunes
# httpx and the typecheck goes 0 errors to 5.
dev = [
    "repld-tool[pretty,browser]",
    "httpx>=0.27",
]

[tool.uv.build-backend]
module-name = "repld"

[build-system]
requires = ["uv_build>=0.11.6,<0.12.0"]
build-backend = "uv_build"

[tool.basedpyright]
typeCheckingMode = "standard"
