# Changelog

All notable changes to this project will be documented in this file.

Format follows [Keep a Changelog](https://keepachangelog.com/en/1.1.0/).

## [Unreleased]

### Upgrading from 0.1.x

Three things to do, none of which repld can do for you:

1. **Re-register the MCP server, once per project:** `claude mcp add repld -- repld bridge`. Then delete that project's `.mcp.json` and the `<!-- repld:start -->` block from its `CLAUDE.md`. `repld init` is gone and repld no longer writes anything into a project.
2. **Rename your bootstrap:** `mv repl.py repld_init.py`, and drop `--init` from any launch command. The file is found by name now, on every kernel — including the ones you don't start yourself.
3. **Migrate any gist still declaring `__repld_tools__`** — it is now ignored rather than warned about, so the file quietly loses its tools. Give each argument its own typed parameter on a `_tool_*` function, move the tool description to the docstring's first line, and put per-parameter descriptions in `Annotated[T, "..."]`. `repld gist lint` is the only thing that will tell you.

Two more, if they apply to you:

- **If your system has no `XDG_RUNTIME_DIR`** (macOS, most containers, any env-scrubbed spawn): stop running kernels *before* upgrading. Their state lives under the old `/tmp/repld` path and the upgraded CLI will not find it — the kernel keeps running and gets replaced by a fresh one, leaving a stale directory to delete.
- **If you only ever run headless kernels:** gist dependencies now need one deliberate install, because a kernel with nobody watching no longer answers its own consent prompt. Start `repld` in a pane once to be prompted, or run the `uv pip install --target …` command it prints.
- **If you bookmarked a dashboard URL:** it will answer 401 now. The page carries an API token, so it is no longer served without one — open it with `repld dashboard` (or `repld dashboard --print` for the URL), which supplies the token from the kernel's private state file. Ephemeral ports meant bookmarks never survived a restart anyway.

`repld[browser]` now requires Chrome 140+.

### Added

- **Lazy kernel spawn.** Opening a Claude Code session no longer spawns a kernel by itself: MCP discovery is answered from a per-project `kernel.cache` (or a static fallback in a cold project), and only a real `tools/call` or a gist/browser `resources/read` triggers a spawn. A kernel that is already running is still attached to for free.
- **Headless kernel.** `repld bridge` spawns a detached kernel on demand, so starting `repld` by hand is now optional — do that when you want the live TUI display. The kernel persists until explicitly stopped, so in-memory state survives a Claude Code restart.
- **Self-healing bridge.** If the kernel dies mid-session the bridge answers each orphaned request with `-31001`, then respawns and replays the client's cached handshake so tools and channel pushes keep working. Liveness is probed before forwarding, never by retrying — `exec` runs arbitrary code and must never run twice.
- **One kernel per project, enforced by flock.** A second `repld` in the same project prints a note and exits 0, so a racing bridge or human connects to the incumbent instead of failing.
- `repld log [-n N] [-f] [--json]` — replay or follow a headless kernel's activity from any terminal, rendered as the TUI renders it. Backed by a new NDJSON event log; `--no-display` mode previously discarded every event.
- `repld status [--json]` — this project's kernel (pid, uptime, socket, dashboard port, active tasks/tickers) plus every live kernel elsewhere, so auto-spawned kernels don't accumulate unseen.
- `repld stop [--all]` / `repld restart` — lifecycle control for kernels you never started.
- `repld dashboard [--print]` — resolve the running kernel's dashboard port and open it, printing the URL when a browser can't be opened.
- `python -m repld` entry point (`src/repld/__main__.py`), which is how the bridge spawns kernels.
- **The kernel runs as its own systemd user service where systemd is available**, so it gets its own cgroup and lifetime rather than being a child of whoever spawned it, and its output reaches the journal (`journalctl --user -u 'repld-<slug>-*'`) — the only place a bridge-spawned kernel's boot failure can be read. Any systemd failure falls back to the previous detached-child path; `REPLD_MEMORY_HIGH` and `REPLD_OOM_SCORE_ADJUST` are opt-in and change nothing by default.
- **The kernel binds itself to the project's virtualenv**, re-execing through `uv run` before boot so code in `exec` can import the project it sits in — which a uv-tool install could not do, its interpreter being fixed by the console script's shebang. This, not a gist declaring `__repld_deps__ = ["."]`, is why a globally-installed repld reaches a project's dependencies.
- **A version-matched venv appearing after boot is adopted live; a mismatched one is refused.** Splicing a mismatched venv half-works — pure-Python and `abi3` packages import, compiled ones don't — so `sys.path` is left alone and the failing import explains why, naming the venv, both Python versions, and `repld_restart`.
- **`repld_restart` MCP tool** — restart the project's kernel from inside the conversation; the MCP session survives, in-memory state deliberately does not. Served by the bridge, since a kernel cannot answer "restart yourself" without the reply dying in flight.
- **`repld_init.py` — a project bootstrap that every kernel runs**, whoever started it, replacing the `--init` flag (see Removed). It runs *after* the socket binds so lazy spawn's 5 s window isn't spent on it, pushes `init_loaded` or `init_error` when it finishes, and never takes the kernel down on failure — a broken bootstrap is exactly when you need a live kernel.
- **Per-parameter descriptions for gist tools, via `Annotated[T, "..."]`.** A typed `_tool_*` function spends its docstring's first line on the *tool* description, leaving nowhere to say a parameter is a date in `YYYY-MM-DD`; bare-string `Annotated` metadata now becomes that property's `description`. Purely additive, and `repld gist new` scaffolds with it.
- **`every(seconds, delay=...)`** holds the first tick back. The default immediate tick is right for polling something that already exists and wrong for watching something you just started, where a false negative at its most fragile moment can send a re-raise loop after something that was about to be fine.
- **`load_dotenv()` is public** — `from repld import load_dotenv`. `./.env` is read once at boot while `./gists` reload on mtime, so a value written afterwards was invisible with no way to ask for it; existing vars still win, so a name captured while empty must be cleared before re-reading.
- **`repld gate` — see and answer the human gates a kernel is blocked on.** Only the TUI's stdin reader and a pinned tab's pill could answer before, and a kernel spawned *for* you has neither, so `confirm()` blocked forever on what lazy spawn made the common case. `repld gate answer <id> <value>` resolves one over the same IPC socket; deliberately not an MCP tool, since an agent answering its own `confirm()` defeats the primitive.
- **The `awaiting_human` channel push now says when nobody can answer it**, carrying the literal `repld gate answer <id> <value>` command — the agent is what reads the push and the human is what needs the command. Gates also carry their id into `repld log -f`.
- **`repld gist fetch <gist-url>`** — download a GitHub gist's `.py` files into `./gists`, or `~/.repld/gists` with `--global`, stamped with a `# source:` header. It is `new`'s sibling rather than `add`'s: a snapshot you then own and edit, where `add` records a link and keeps tracking the file it points at. `--name` renames a single-file gist, `--force` re-fetches over your copy, and the three collision scopes `add` enforces (local, global, already linked) apply here too. Declared `__repld_deps__` are reported but not installed — `add` links code you wrote, this is code from a URL, and the next kernel boot prompts for them anyway.
- `repld gist lint --local` — restrict the check to `./gists`. The default scope spans every gist a kernel here would import, which is right for a manual sweep but makes the exit code useless as a CI gate when a linked gist from another repo fails the run.

### Changed

- **`repld/browser/__init__.py` split into `browser.py`, `pool.py` and `target.py`**, leaving the package `__init__` as re-exports. It was 990 lines holding `Browser`, `BrowserPool` and `LazyBrowser` inline — the one module that never got the treatment `tab.py` had when `tab_query` / `selector` / `row` / `pin` were carved out of it. Every public import path is unchanged, and `make_target` now sits in a leaf module, so `tab.py` and `observe.py` take it at module level instead of a call-time `from . import make_target` to dodge a cycle.
- **Duplicated derivations given owners.** The JSON-RPC envelope builders moved to `core_schemas` (the bridge hand-built them at ten sites and cannot import `protocol`), and the bridge now reads a doc resource's `mimeType` from the same declaration it advertises rather than a literal. The two gist directories are `paths.global_gists_dir()` / `local_gists_dir()` / `gist_dirs()` instead of fourteen hand-written copies, one of which carried a hand-sync comment. `gates.yes_by_default` replaces two private `("", "y", "yes")` sets that accepted less than `gates.parse_response` does, so `1` and `true` declined a boot prompt while confirming a gate.
- The kernel's boot banner advised `claude mcp add -s project`, the scope that writes a `.mcp.json`. It prints the plain local-scope form now.
- **`exec` no longer runs a full garbage collection after every cell** — 164 ms per cell against 800k live objects, in a runtime whose premise is holding large state for weeks. It is a generation-0 collection now (~0.005 ms), which is where an abandoned coroutine actually is.
- `browser.get(id, timeout=…)` now bounds the target-ID path too. It was documented as ignored there, which since the attach-race retry became a proper deadline loop meant a five-second floor on a call asking for one. An ID matching no target still fails immediately either way.
- The browser tool timings that were bare literals are named: `_SETTLE_NAVIGATION_S` / `_SETTLE_INTERACTION_S` / `_SETTLE_DEFAULT_S` in `browser_dispatch`, plus the two unexplained 0.3 s pauses (`_POST_TYPE_DEBOUNCE_S`, `_POST_REATTACH_SETTLE_S`). `cdp._INFLIGHT_MAX_AGE` justified itself against `observe.settle`'s 5 s default, which no caller passes; it now names the real ceiling, the 8 s navigation settle.
- The HAR, console, SSE and lifecycle views no longer end in an `ORDER BY` every caller already overrides — 35.4 ms → 30.4 ms at 40k events on the `har_summary` delta `post_observe` runs per tab per mutation. It was also the thing that made an order-dependent query look correct until its result set outgrew its `LIMIT`, which is exactly how `Tab.sse()` / `Tab.lifecycle()` once returned the *oldest* 500 rows.
- **The IPC socket is created 0600 rather than chmod-ed to it**, closing a window in which it was connectable by any local user. The 0700 project directory was and remains the actual barrier, so this is defence in depth rather than a live hole.
- **Runtime state moved out of the project directory** into `$XDG_RUNTIME_DIR/repld/projects/{basename}-{sha256(realpath)[:8]}/`, created 0700. Every file is the socket path with a different suffix (`.lock` / `.flock` / `.dashboard` / `.events`), so `--socket` and `REPLD_SOCKET` move the whole set together; the `./.pyrepl.*` fallbacks are gone.
- **Channel pushes caused by a specific request now go only to the session that made it**, `defer()` included. Genuinely ambient pushes stay broadcast (`@every` errors, console errors, browser connect/disconnect, bare `notify()`), and a push whose origin disconnected is dropped rather than downgraded to a broadcast that would leak one session's output into every other.
- **A headless kernel no longer builds the display event queue at all.** It was initialized anyway and a drain thread started solely to discard it, costing two lock round-trips and a cross-thread wakeup per event — several per `print()`. No behavioural change: the event log, `repld log`, and channel pushes are fed by the sink, which always ran first.
- The MCP `initialize` response declares `listChanged: true` for tools and resources, a prerequisite for the bridge's post-respawn notifications. `CAPABILITIES` and `PROTOCOL_VERSION` now live in one module, since `initialize` has three authors — kernel, bridge, and `repld exec` — and a drifted capability *disables* a feature rather than degrading it.

### Security

- **`atomic_write_json(chmod=…)` creates its temp file at that mode instead of chmod-ing it afterwards.** The temp briefly sat at 0644 holding byte-for-byte what the final file would — the dashboard's API token, the session registry, the kernel lockfile — which is the exact thing the rule exists to forbid. Every affected file lives under a 0700 directory, so this is defence in depth.
- **A headless kernel no longer installs gist dependencies without being asked.** The `[Y/n]` prompt read a bare Enter as yes, and an auto-spawned kernel has stdin on `/dev/null` where `readline()` returns `""` immediately — so the prompt degraded into unattended consent on what is now the default spawn path, for any reachable gist's `__repld_deps__`. The gate is a genuine tty check now, and a kernel that can't prompt reports what's missing and prints a copy-pasteable install command. **Headless-only users need one deliberate install; at a real terminal nothing changes.**
- **The dashboard's `POST /api` caps its request body at 1 MiB.** `Content-Length` was read unbounded and handed straight to `readexactly()`, so a single header claiming 10 GB had the kernel allocate toward it — and the 5 s read timeout is no defence against a sender on loopback. The cap sits *after* the Bearer check, so an anonymous caller still gets 401 and cannot probe for the limit. Holding the token is not licence to OOM the process the token exists to protect.
- **Runtime state is no longer world-readable when `XDG_RUNTIME_DIR` is unset** — macOS, most containers, any env-scrubbed spawn. `Path.mkdir(mode=0o700)` applies the mode only to the leaf, so `/tmp/repld` came out 0755 and task spills, screenshots, and the session registry were created 0644; the fallback root is `/tmp/repld-{uid}` now, created 0700 and refusing a root another user owns. **Stop running kernels before upgrading if this applies to you** — see Upgrading above.

### Fixed

- **An `@every` ticker's output was attributed to the cell that registered it, and then thrown away.** Applying the decorator inside an `exec` cell schedules the ticker from that cell's context, so it inherited the cell's task id via `copy_context()` and kept it for the life of the kernel. Every output cap keys on that id and is cleared only by the owning cell's completion, which happens long before the second tick — so a ticker's `print()` filled a *finished* cell's 4 KB budget and was then dropped permanently by both the live display and `repld log`, which on a headless kernel is the only surface there is. Its writes also went into that cell's spill file until the hourly sweep unlinked it, after which they went nowhere at all. Tick output is ambient now, rendered unattributed and uncapped. A `defer()` from a tick body is likewise ambient again, rather than inheriting a disconnected session's completion route and being dropped.
- **The label bar stopped re-mounting after a browser reconnect.** `Page.addScriptToEvaluateOnNewDocument` is session-scoped exactly like the gate binding beside it, but the restore lived only in `Tab._reattach` — the navigation/HMR path — so a WebSocket `_reconnect` (sleep, network blip, Chrome restart) dropped it. Invisible when it happened, because the bar is live DOM and survives on the current document; it just never came back on the next navigation, which is the whole reason the registration exists. `_reattach_core` re-applies it, next to the binding whose identical bug it outlived.
- **`since=` meant three different clocks across four methods on the same object.** `tab.network(since=)` compared epoch seconds, `tab.console(since=)` epoch *milliseconds*, and `tab.sse(since=)` / `tab.lifecycle(since=)` a monotonic counter from an arbitrary origin — so `tab.console(since=time.time())` returned everything rather than nothing, silently, and the two monotonic ones returned nothing. It is epoch seconds everywhere now (pass `time.time()`), converted per view; `docs/browser.md` carried this as an open design question and now records the decision.
- `browser.get("9222:abc123")` on a cold pool failed with "No browser on port 9222. Connected: []" — the target-ID path skipped the `_ensure_any()` auto-connect the glob path does, so a lazy pool refused the more specific of its two argument forms.
- **`browser_invoke` never returned the control's result.** The handler ran `tab.invoke(...)` inside an observation wrapper and discarded its return value, so the one thing the tool exists for — what `window.controls.invoke()` gave back — reached nobody, while the tool description advertised `{returned, stateBefore, stateAfter, duration}`. It returns `{result, observation}` now: the control's payload *and* what it did to the page.
- **A failed event-store insert could hang a paused request in Chrome.** `CDPSession._handle_event` stored the event before dispatching `Fetch.requestPaused` (the only thing that ever resumes a paused request) and `Runtime.bindingCalled` (how a pill answers a human gate), with one `except Exception: logger.debug(...)` around the lot. A raise from the store — a closed DuckDB after a torn-down session — skipped both, leaving the request hanging with `settle` waiting on it and swallowing the gate answer, at debug level. The store is guarded on its own now: losing an event is a gap in history, losing the resume wedges the tab.
- **`ready="main"` (or any bare tag or custom element) silently never matched.** `Tab._await_ready_signal` classified with its own `startswith((".", "#", "[", "data-"))` instead of deferring to `selector.py`, so a bare name took the JS branch, evaluated as an identifier, came back a ReferenceError whose `result.value` is merely absent, and polled for the full 10 s — while `ready="#app"` worked. Classification is shared with `click`/`type_text` now (so `text=`, `role=`, `label=` and `:has-text()` work here too), and an expression that *raises* is reported immediately rather than polled out.
- **The dashboard read an unbounded number of request headers, before any authentication.** The header loop ran ahead of the Host check and both auth paths, with a per-line timeout that reset every line and no cap on the count — one connection was measured feeding 200,000 headers in without the server ever answering. Capped at 100 lines with a 431, the same reasoning `_MAX_BODY_BYTES` already applied to the body, which at least sat behind the token.
- **A stale bridge reader could tear down a freshly attached kernel.** `_read_kernel` compared its generation under the state lock, released it, and *then* called `_on_kernel_gone`, which unconditionally nulls the socket. A reconnect completing in that gap left the superseded reader closing the replacement's socket and answering every id registered against it with `-31001`. The check happens inside the teardown now, under the lock that performs it.
- **A failed reconnect leaked every tab's event store, and a partly-failed one left a live session with a closed one.** `BrowserSession._reconnect` cleared `_sessions` before reopening the WebSocket, so a genuinely-down Chrome dropped the saved `CDPSession`s without `cleanup()`; and `_reattach_core` registers a session *before* enabling its domains, so a raise from any of those left it registered and attached in Chrome with its DuckDB closed underneath.
- **Closing a tab mid-query surfaced as `browser_network: Connection Error`.** `CDPSession.cleanup()` closed the DuckDB from the loop while `query`/`query_dicts`/`clear_events`/`fetch_body` take per-call cursors specifically so they can run off it. The close now waits out an in-flight read and marks the store closed, so a reader arriving afterwards gets an error that names the tab.
- **A failed `Fetch.disable` was reported as success.** `capture.disable` swallowed its own exception and cleared the handler regardless, making `CDPSession.disable_fetch`'s rollback unreachable — and clearing the handler while Chrome is still intercepting hangs every subsequent request on that tab. It propagates now, and clears the handler only once Chrome has confirmed.
- **`repld bridge` accepted any argv at all.** It threw `resolve_socket_path`'s residue away, so `repld bridge --help` started a proxy blocking on stdin and a typo'd flag in an MCP registration (`--sockett`) was silently ignored — on the one command that has no terminal to notice. It validates like every other subcommand now, as does `repld gist <unknown-verb> --help`, which printed "unknown command" instead of usage.
- **A wrong-shaped gist registry crashed three callers.** `gists._read_registry` returned whatever JSON the file held; a root that was a list or a string then reached `.items()` / `.values()` in `repld gist list`, `gist_links.link_targets`, and the `repld://gists/_registry` resource. Non-dict roots and non-UTF-8 files now read as empty, the guard its sibling `gist_links.read_links` always had.
- **The pane's stdin claim was never released.** `gates._stdin_owned` was set true at display start and never set back, so once the reader thread ended (EOF on a redirected stdin, an OSError) `tty_prompt` declined to ask for the rest of the process's life — and `gist_deps.install_deps` blamed a thread that had exited, pointing at a `repld restart` that would do it again. A gate could also leak permanently into `gates._gates` if anything raised between registering it and the `try` that pops it.
- **`get_task` could answer `-32603` for a task whose spill had just been evicted.** `tasks.snapshot()` read the spill file unguarded while its sibling `preview_since` had always caught this; `_prune_spill_files` pops the registry entry under the lock and unlinks outside it, so a poll landing in between got a `FileNotFoundError` out to the agent as an internal error rather than as missing output.
- **The loop watchdog cancelled an arbitrary task while reporting it as the longest-running one.** Its fallback sorted `Task-N` names lexicographically, so `Task-10` came before `Task-2` — wrong exactly when a human is debugging a wedged loop and trusting the name in that push. It sorts by the numeric suffix now, which is monotonic in age.
- The dashboard page's `esc()` was `textContent → innerHTML`, which escapes `& < >` but never quotes, and it is used in attribute position throughout (`title=`, `href=`, `data-origin=`, `value=`) with tab URLs, page titles and project paths. Responses also now declare `Connection: close`, which is what the server has always done.
- `ipc.connect_to_kernel` dropped its socket unclosed on a failed connect — 50 of them per bridge respawn, each with a `ResourceWarning`.
- `state.acquire_lock` could bring `RUNTIME_DIR` into being as an implicit parent at 0755 under `--socket`/`REPLD_SOCKET`, skipping `ensure_runtime_dir`'s mode and foreign-owner check; `_claim_project` ensures the root first.

- **Off-loop fire-and-forget work waited on the watchdog to run at all.** `bg.spawn(loop=…)` — the path `tab.label = …` and `tab.capture_bodies = …` take from any pure-sync exec cell, which runs in a thread — created the task with `loop.create_task`. From a foreign thread that queues the task's first step without waking the loop, so on an idle loop the coroutine never ran; what made it look fine was the loop watchdog's once-a-second probe, whose wakeup drained the queue. It also raised outright under `PYTHONASYNCIODEBUG=1`. The task is created on the loop now, keeping the `repld-` name that excludes it from the watchdog's own kill list.
- **`browser.clear()` from an exec cell could lose an event-count increment.** `clear_events` reset `_event_count` / `_next_prune_check` / `_inflight` from whatever thread called it, racing the `+=` that `store_event` does on the loop — leaving the FIFO prune trigger describing a store it no longer matched. The MCP tool already went through the loop; the exec-cell and dashboard routes now do too.
- **`repld gate answer` mangled an answer that looked like one of its own flags.** Every flag parser reached past the gate id: `--socket PATH` is consumed from anywhere in argv, `--json` was filtered the same way, and `--help` is scanned for in every argument. So `repld gate answer <id> deploy --socket now` answered `"deploy now"` with the connection silently repointed at a kernel called `now`. Everything after the gate id is the answer verbatim now, and flags for the command go before `answer`.
- **A pinned tab's pill stopped answering human gates after a browser reconnect.** CDP bindings are scoped to the session that registered them, so Chrome drops `window.__repld_resolve` from the page the moment that session detaches — and only `Tab._setup_binding` ever added it. A reattach caused by *navigation* recovered on its own (the pill goes with the document, so the heartbeat re-injects), but a WebSocket reconnect — laptop sleep, network blip, Chrome restart — reattaches every session while leaving the pages untouched, so the heartbeat saw a live pill and never re-injected. The buttons then called an undefined function, having already cleared the gate from their own queue: a gate that silently could not be answered from the surface showing it. `_reattach_core` re-registers the binding, alongside the Fetch capture it already restored.
- **A failing kernel respawn could print its explanation fifty times.** The bridge's restart poll passes `quiet=True` to suppress the retry chatter, but that flag stopped at the bridge's own stderr call — every line that says *why* a spawn failed lives in `spawn.py` and was unreachable from it. `quiet` now reaches them. The same poll also re-ran the full spawn (a `systemd-run` plus a `systemctl is-active` subprocess) on every 100 ms tick; it retries every 500 ms now, which is what the code already claimed and what the 5 s window was budgeted for.
- **`repld log` could drop a truncated cell's elision notice.** When a cell's output filled the per-cell viewer cap exactly on a chunk boundary, the event log flagged the following (empty) chunk as truncated and the reader discarded it for having no text — so the cell appeared complete with nothing saying otherwise.

- **`tab.label` never survived a navigation, which is the only reason it exists.** The label bar is registered with `Page.addScriptToEvaluateOnNewDocument` so it re-mounts on every document — but that script runs at document *start*, where `document.body` is still null, so the mount threw before appending anything. It looked like it worked because `runImmediately: True` also evaluates against the currently-loaded document, so the bar appeared when you set it and then silently failed to come back. It waits for `DOMContentLoaded` when there's no body yet.
- **Fire-and-forget async work could be garbage-collected mid-flight.** `asyncio` keeps only a weak reference to a running task, so the ten `create_task` calls whose handles nothing kept could vanish before finishing — silently, and attributable to the browser rather than to repld. The worst was the `Fetch.requestPaused` handler, the only thing that ever resumes a paused request: losing it left the request hanging in Chrome until it timed out, with `settle` waiting on it. Also reachable were a tab attaching but recording no events (`_enable_domains`), `watch()` skipping a tab (`_auto_attach`), and a human gate rendering no pill on a kernel where the pill was the only surface that could answer it. New `bg.spawn` holds the reference until the task completes.
- **URL filters over-matched on `_` and `%`.** `network(url=…)` and `sse(url=…)` translate `*` to a SQL `LIKE` wildcard but left LIKE's own metacharacters live, so `_` matched any single character — and it is common enough in URLs that `/api/user_profile` also matched `/api/userXprofile`. Escaped now, paired with an `ESCAPE` clause on the comparison; `*` is still the only wildcard.
- **Selectors disagreed about whether a hidden element counts.** `text=` was the only form that considered visibility, so `text=Save` skipped a hidden button while `role=button[name="Save"]` returned it and the click landed on nothing. All four custom forms now rank visible matches above hidden ones rather than filtering — an invisible match is still a match, since apps do keep the real control off-screen behind a styled proxy.
- **`BrowserPool`'s multi-browser fan-out could die on "dictionary changed size during iteration".** `disconnect`, `pages`, `watch`, `detach` and `get` all iterate `self._browsers` with an `await` in the loop body, and an await is a yield point: another task running `connect()` or `disconnect()` on the same loop mutates the dict mid-walk. Reachable from the dashboard, whose connect/disconnect RPCs are `async def` on that loop — a Connect click landing during a `browser.watch("*x*")` sweep was enough. All five snapshot with `list(...)` now, like their sync siblings. `disconnect` additionally removes the snapshotted ports one by one instead of calling `clear()`, which would otherwise drop a browser connected mid-sweep out of the registry with a live WebSocket nothing would ever close.
- **A cleanly closed Chrome socket stalled every in-flight CDP command for 30 s and skipped auto-reconnect.** `_recv_loop` failed its pending futures only from its `except` block, but `websockets` swallows `ConnectionClosedOK` and ends the iteration normally — so on a polite close (1000/1001) nothing resolved them, each caller waited out its full `wait_for`, and `TimeoutError` is deliberately not recoverable, so the reconnect-and-retry that an *abrupt* close gets for free never ran. The tidier failure was the worse one. Both endings now fail `_pending` with the same recoverable wording; a cancellation from a deliberate teardown still doesn't, since `disconnect`/`_reconnect` handle those futures themselves.
- **Captured response bodies never counted toward the event store's FIFO cap.** `_event_count` was incremented in `_handle_event`, while `capture.py` writes its synthetic body rows by calling `store_event` directly — so the rows that dominate the store's memory, at up to 500 KB each, were the only ones invisible to the prune trigger. The counter moved into `store_event`, and the trigger became a threshold rather than an exact-multiple test, which a counter that can now step by more than one would skip straight past.
- **A glob `browser.get()` could fail on a tab it was about to attach.** When a target matches both the glob and a watch pattern, repld's own auto-attach claims it first and `attach()` answers `None`; `_get_by_glob` read that as a miss and raised `TabNotFoundError` immediately. It waits the race out now, the way `get()`-by-id and `open()` already did, bounded by the caller's own timeout so a `timeout=` still caps the total rather than applying per matching target.
- **Concurrent browser connects each opened their own WebSocket.** `Browser._ensure_connected` and `BrowserPool.connect` both checked-then-acted across an await, and `BrowserSession.connect` rebinds `_ws`/`_recv_task` unconditionally, so N callers arriving together produced N sockets and N recv loops with only the last reachable — the rest left open against Chrome for the life of the kernel, each a second debugger client. Two parallel `browser_*` tool calls on a cold pool is enough, since the connect yields for milliseconds fetching /json/version. Both paths are locked now (`BrowserSession` already guarded the *re*connect the same way); the pool also reuses a disconnected `Browser` rather than replacing it, so its CDPSessions and event stores survive.
- **Two browser tools walked loop-owned state from the IPC thread.** `browser_tabs` and `browser_clear` were the only handlers not routed through `_run_async`, and both iterate the session/browser maps the loop mutates on every tab that opens or closes — one popup mid-call and the tool dies with "dictionary changed size during iteration". `browser_clear` additionally reset `_event_count` and emptied `_inflight` from that thread, the latter readable by `settle` on the loop. Both run on the loop now. The sync browser API also snapshots those maps before walking them, because `runtime._eval` runs every pure-sync exec cell in `asyncio.to_thread` — so `browser.tabs` typed into a cell was already off-loop, independent of MCP.
- **`tab.capture_bodies = True` immediately followed by `= False` could settle inconsistently.** The setter is fire-and-forget, so the two tasks interleaved and could leave `capture_bodies` true with Fetch actually disabled. Serialized per session. The lock is taken by a wrapper rather than the body, because `Fetch.enable` can trip a reconnect that re-enters through `_reattach_core` on the same task — reentrancy that would deadlock outright, the same reason `reattach_session` wraps `_reattach_core`.
- **A gist declaring a new dependency mid-session could wedge the kernel.** `install_deps` prompts for consent on the real terminal, which is correct at boot and nowhere else — but it is also reached from `gists._check_reload` / `_scan_new_deps`, i.e. from `builtins.__import__` inside a user cell, long after the display thread has parked in `readline()` on that same stdin. Two readers on one stream: the display's reader holds the buffer lock and discards the answer (no gate is open), while the prompt waits behind it. On an `await` cell — which runs on the shared loop rather than in a thread — that is the whole kernel, and the watchdog's 30 s cancel can't land because a blocking `readline()` offers no await point to deliver it at. `tty_prompt` now declines outright once `display.run_display` claims stdin, putting a late caller on the same path as a headless kernel; the boot-time prompt, before the display exists, is unaffected. The skipped-install message names which of the two reasons applied.
- **Every asset body on a captured tab made a needless round trip through CDP.** Fetch interception has to register on `*` (Chrome offers no resource-type filter on `Fetch.enable`), and the response-stage handler only ever skipped redirects and SSE — so every image, font, stylesheet, script and media response on a `browser.get()`/`open()` tab had its full body pulled over the DevTools WebSocket as base64 and pushed straight back via `fulfillRequest`, since `getResponseBody` consumes the buffer and a plain continue would deliver an empty response. That is a round trip plus roughly 2.7× the resource's bytes added to page-load latency, for rows `tab.network()` hides by default and the observation bundle collapses to a byte count. Assets are now skipped up front, matching `har_entries`' own `is_asset` derivation exactly; `tab.body()` still fetches one on demand via `Network.getResponseBody`.
- **`tab.unpin()` could strand a tab pinned forever.** It cancelled the heartbeat before removing the pill, so if the tab was already gone the `Runtime.evaluate` raised and left `_pinned` set with nothing left to clear it — the heartbeat is the only other writer. A later `pin()` then saw the flag, took the reason-only branch, and never re-injected. State now clears in a `finally`, mirroring what `pin()` already did on the way in.
- **A redirect chain reported the wrong status on its final hop.** `tab.network()` showed a 302 → 200 login as `302` with `state: complete` beside it — the one case the per-hop HAR view exists to make visible. Chrome fires `Network.requestWillBeSentExtraInfo` / `responseReceivedExtraInfo` once per hop under a single requestId, and both were collapsed with `MAX(...) GROUP BY request_id`; on a VARCHAR status that is a lexicographic pick across hops, so `302` beat `200` (and `307` beat `200`, while `301` → `404` came out right by luck). The same collapse pulled each hop's request headers from whichever hop's JSON sorted highest, so `tab.request()` could show hop 1's `Cookie` on hop 2. Both are numbered per hop and joined on `redirect_index` now. Single-hop requests emit one event each and were never affected, which is why it survived this long.
- **`tab.request()` dropped five fields it was documented to return.** `curl_command`, `auth_cookies`, `request_cookies`, `loader_id` and `frame_id` were all computed by the view and then discarded on the way out, so the reconstructed curl — the headline reason the column exists — was reachable only by hand-writing SQL against `har_entries`. The docs also described the result as a flat superset of a network `Row`; it is a nested dict, and now says so.
- **A gate that timed out left the kernel's pane unable to answer any other.** `ask`/`confirm`/`choose` accept `timeout=` — the documented way to keep a gate from parking a cell indefinitely — but an expiring gate resolves nothing, and the pane only ever dropped a gate on the event that fires when a human *answers* one. The expired entry stayed, stayed newest, and the stdin reader routes to the newest: every line typed afterwards silently resolved a gate the kernel had already dropped, and the dead prompt was re-shown whenever a later gate was answered. A cancelled cell ended the same way. Both now emit `HumanPromptClosed`, which the pane and `repld log` render as `↳ no response (timeout | cancelled)`.
- **`repld help gists --help` printed the gists topic instead of usage.** `help` was the one subcommand still checking `argv[0]` alone for `-h`/`--help`, and the one that ignored arguments it doesn't understand. It goes through `cli_args` with the others now.
- **`repld gist fetch` and `repld gist list` were the last two verbs still open-coding their own argument checks.** `fetch` refused an unknown flag in a wording no other subcommand uses (`error: unknown flag '--x'` against everyone else's `repld gist fetch: unknown argument '--x'`), and reported a surplus positional as bare usage with no reason given; `list` carried a byte-identical copy of the surplus-positional check `cli_args` owns. Both go through `cli_args.check_args` now — which is the whole point of that module, and it was extracted from this file.
- **`repld dashboard` left the API token in shell scrollback** on the success path, having just handed it to the browser. It reports the port instead, like the boot banner and `repld status`; `--print` is still there for when you want the URL.
- **A cold-start session was never told the gist registry existed.** `repld://gists/_registry` was declared only in the kernel, so a project with no kernel and no `kernel.cache` got a `resources/list` missing it — the same two-author drift `core_schemas` exists to prevent. It is declared there now, beside the docs but deliberately not among them, since it has no `help.py` text behind it.
- **`repld gist <verb> --help` disagreed with `repld gist --help`** for four of the six verbs, each having kept its own copy of a usage string the command table was supposed to be the single source of. Both are composed from that table now.
- **`repld status` could crash on a malformed dashboard hint file.** A hint whose JSON parsed to something other than an object reached `.get()` on a non-dict, and the surrounding `except` didn't cover it. The three copies of that read — `repld dashboard`, `repld status`, and the dashboard's own cross-project sidebar — were each catching a different set of exceptions; they now share one reader, in which absent, unreadable, malformed and token-less all mean the same thing.
- **One unexpected exception in the bridge ended the MCP session.** `run()`'s stdin loop caught only `BrokenPipeError`/`OSError`/`ValueError`, so any `KeyError` or `TypeError` while handling a message — a malformed `kernel.cache` was enough — fell out of the loop and closed the bridge's own stdout, which an MCP client dispatcher can never recover from. The kernel end of the same socket has always answered its own bugs with `-32603`; the bridge now does too, and the in-flight id is answered rather than stranded.
- **`tab.screenshot(full_page=True)` returned a squashed image and dimensions that described something else.** Both the resize target and the reported `source` were measured from the visual viewport, while `captureBeyondViewport` captures the whole content box — so on any viewport wider than 1440px a tall full-page shot was resized down to viewport height, and the `scale` factor documented as mapping back to page pixels mapped back to nothing. Measurement now follows what was actually captured.
- **`tab.control_observations()` always returned an empty list.** An observation is logged as `console.debug("__controls__", json)`, but the `console_entries` view exposes only the *first* console argument, so the payload it tried to parse was never in the row. It now reads the base event store, like the live push path always has.
- **`repld gist list` reported a corrupt `./gists/.links` and still exited 0**, printing a listing that silently omitted every linked gist — so a script gating on it read a truncated answer as a complete one. It returns 1 now, as `add`, `rm` and `fetch` already did.
- **`repld://docs/guide` told agents that a globally-installed repld cannot see project dependencies.** It can, and has since the kernel started binding itself to the project's interpreter — the same document's setup section recommends exactly that install. The advice sent agents toward adding `repld-tool` to project dependencies to fix a problem they did not have.
- **The browser guide showed `browser.pages()`, `.detach()` and `.disconnect()` without `await`**, in a block that awaited the adjacent calls correctly, so the omission read as deliberate. All three are coroutines; as written they did nothing.
- **`kernel.flock` could hold a mangled pid.** The file is deliberately never truncated at open, so a shorter pid written over a longer one left the tail of the old number behind (`99` over `123456` reads as `99456`). Nothing in repld reads it — it exists to be looked at — so it is now truncated before the write.
- **The dashboard's API token gated nothing.** `GET /` served the control panel — with the token embedded in it, since the page needs it — to anyone who asked, so any process on the machine could read the page, take the token, and then enumerate every project cwd via `sessions` or attach to your Chrome through the browser RPCs. Loopback keeps other hosts out, not other users, and every file holding this data is written `0600`. `GET /` now requires the token too, supplied as `?token=` by `repld dashboard` (which reads it from the kernel's private hint file) or by a per-port cookie it sets so a refresh still works after the page drops the query from the address bar. `POST /api` stays Bearer-only — a cookie would ride along on requests you didn't initiate. Sidebar links to sibling dashboards carry each peer's own token, so cross-project navigation is unchanged. **Open the dashboard with `repld dashboard`; a bare `http://127.0.0.1:<port>/` now answers 401 with instructions.**
- **`tab.ask()` on a pinned tab was a gate nothing could answer.** The pill has buttons and no text input, so it renders `confirm` and `choose` and draws nothing for an `ask` — but routing was decided on pinnedness alone, which also suppressed the `repld gate answer <id>` command the push carries when no other surface exists. On a bridge-spawned kernel that left the cell blocked forever with no pill and no instructions.
- **One SSE connection permanently wedged settle on its tab.** A request left the in-flight set only on `loadingFinished`/`loadingFailed`, and a stream emits neither until it closes — so every later mutation on that tab burned its full deadline (5 s, plus 5 s more on a new iframe) and settle stopped meaning "idle" at all. Streamed responses now clear when their headers arrive; anything else still open after 60 s is aged out.
- **A cell that finished in the wrong millisecond was promised a completion push nobody sent.** `exec` marks a task nudged *after* taking a full snapshot of its output, and a cell completing in that window had its push dropped for not being nudged yet — while the reply said completion would arrive via channel. The promise is now taken atomically, and `exec` answers with the result when it couldn't be.
- **`repld exec` exited 0 printing nothing when `get_task` returned an error.** An evicted `task_id` answers `-32602`, which became an empty result and a clean exit; the sibling code path checks, this one didn't.
- **`repld gist add` could silently repoint a name another linked gist depends on.** The manifest is a flat name→path map and every co-linked sibling was written unconditionally, so linking one gist could steal a sibling name out from under a gist already linked against it — reported as nothing more than `+ siblings: <name>`. `new` and `fetch` both refuse this collision; the command that writes the manifest now does too.
- **`repld_restart` could reattach to the kernel it had just killed and report it as the replacement.** A kernel still draining `@every` and `defer()` finally blocks keeps its pid and its bound socket past the wait budget, so the reconnect handed the dying process back as `pid N → N` and left nothing running.
- **The dashboard's Console and Network tabs ran their DuckDB scans on the kernel's shared loop**, stalling every cell, ticker and browser observation for the length of the scan. The panel also fetched its own kernel state exactly once, so uptime, tasks, tickers and the tab table sat frozen at page-load values beside a sidebar that kept ticking.
- **An `@every` cancelled during its inter-tick sleep stayed in the ticker registry forever**, so `every.list()` and the dashboard kept reporting a schedule that no longer existed — precisely when the watchdog had just cancelled it and someone was debugging a wedged loop.
- **A cold-start MCP session was told nothing about gists.** With no kernel and no cache, the bridge served `initialize` with the exec model alone — no `./gists` on `sys.path`, no `_tool_*` registration, no `__repld_deps__`, no playbook — and nothing re-sends `instructions` after the lazy spawn, so it stayed missing for that whole session.
- **A standard MCP `ping`, or one malformed stdin line, spawned a kernel.** Anything the discovery intercept didn't recognise fell through to the spawn-and-wait, defeating lazy spawn for methods no repld kernel implements.
- **`repld log -n 0` printed the entire log and `-n -5` printed everything except the oldest five.** The count is now required to be positive.
- **The `repld gist` verbs disagreed about unknown arguments.** Only `fetch` and `list` validated, so `gist new x --global` scaffolded locally while ignoring the flag its sibling honours, `gist add foo bar` linked only `foo`, and `gist lint --json` went looking for a gist named `--json`. All six reject what they don't understand now.
- **`repld gist lint` created `./gists` and `~/.repld/gists` in a project that had neither**, then reported "no gists found". It also refused a `--local` name whenever a global gist shared the stem, while the no-argument form linted that same local file happily.
- Documentation corrections: the settle loop had been an in-memory count for a while but was still documented as a DuckDB query; `repld help gists` said dependencies install into the venv (they go to a shared, interpreter-versioned dir, and deliberately never the venv); and the channel-kinds table listed a `user` kind nothing emits while omitting `venv`.
- **`curl_command` silently corrupted any request body containing an apostrophe.** The shell-quoting `REPLACE` was written as `chr(39) || '\' || chr(39) || chr(39)`, but the view's SQL lives in a non-raw Python string, so Python ate the backslash and DuckDB compiled `'''` instead of `'\''`. The result was not a broken command — it was a command that still ran, having quietly dropped the apostrophe and everything quoted around it. Spelled `chr(92)` now, and the replacement round-trips through a real shell.
- **`repld gist new <name>` could silently shadow a gist you had linked from another project.** It checked only for a local file of that name, while its sibling `repld gist fetch` enforced all three scopes (local, global, already-linked). Since a local gist deliberately shadows a linked one at import time, scaffolding onto a linked name replaced working code with a stub and said nothing. Both commands share one collision check now.
- **`repld gist <verb> <name> --help` ran the verb instead of printing usage**, because the gist group's help check looked only at the first argument — so `repld gist rm foo --help` unlinked `foo`. It scans every argument, like the rest of the CLI.
- **`repld gist fetch` tracebacked on a corrupt `./gists/.links`** rather than reporting it the way `add`, `rm` and `list` do.
- **`repld exec` exited 1 with an empty stderr if the kernel died while fetching a finished task's output** — the one moment where silence costs you the result you were waiting for.
- **`__repld_deps__ = ["."]` installed a dependency the kernel could never import.** An editable install lands in the `--target` dir as nothing but a `.pth`, and `ensure_deps_on_path()` used a plain `sys.path.append`, which never processes those — so the one dep form that exists for the *unbound* case was the one that didn't work there. It uses `site.addsitedir` now, which still appends, so a project's own packages keep winning.
- **The first `exec` after a lazy spawn could race the project bootstrap**, reaching a bare `__main__` and raising `NameError` for anything `repld_init.py` defines. Cells now wait for the bootstrap — released unconditionally however it went, since a kernel nobody can run code in cannot be repaired.
- **…and so could a gist or browser tool**, which is the *other* thing that lazily spawns a kernel — `tools/call` is what triggers the spawn, and the socket binds before the bootstrap runs. Only `exec` waited, because only `exec` can degrade a long wait into a `task_id` plus a channel push; the tools that answer inline or not at all now block on the same flag, bounded by the 30 s the bootstrap itself gets.
- **A browser or gist tool that outran its 30 s deadline reported an error with no message.** `concurrent.futures.TimeoutError` *is* `TimeoutError` and carries an empty string, so the tool name, a colon and nothing was the whole answer — for work that can legitimately take that long (`browser_js` awaiting a promise, `browser_fetch`, `browser_cdp`, any async gist tool). The deadline is named now, along with the fact that the call was abandoned rather than cancelled.
- **`repld status` printed dashboard URLs that answer 401.** When `GET /` became authenticated the boot banner switched to naming the port and pointing at `repld dashboard`; `repld status` kept printing the bare URL, for its own kernel and for every sibling. A sibling's dashboard needs *that* project's token, so the way in is its own `repld dashboard`, not a link printed here.
- **Every observed browser mutation stalled the kernel loop with DuckDB queries, before and after.** `pre_observe` took its delta cutoff from the `har_entries` / `console_entries` *views*, evaluating a whole CTE chain (22 ms at 2k events, 82 ms at 40k) once per tab plus once per iframe, and `post_observe` then ran `har_summary` on the loop for another 39–56 ms per tab. The snapshot became a single `MAX(rowid) FROM events` (an equivalent cutoff, ~1.5 ms) and the post-mutation deltas moved off the loop entirely; worst-case stall drops from 104 ms to 7 ms.
- **`BrowserSession.disconnect()` leaked every attached tab's event store**, closing the WebSocket but leaving each tab's in-memory DuckDB connection open — and `BrowserPool.connect` replaces the whole Browser on reconnect, so nothing ever came back for them. Every other teardown path already called `cdp.cleanup()`.
- **An iframe observation reported unrelated tabs' modals as its parent's.** `_detect_parent_dialogs` evaluated JS in every attached page-type target at one round trip each; it resolves the real parent through `parentFrameId` now.
- **A second open gate re-prompted with placeholder text instead of its question**, showing `? still waiting [y/n] #a1b2c3` — and the pane is the only thing telling the human what is being asked. The prompt is kept alongside the kind and options now.
- **One unreadable file no longer tracebacks the whole `repld gist lint` run.** A non-UTF-8 `.py` raised out of `lint_file`, and the default scope spans gists belonging to projects you never opened; it reports as a `syntax` finding instead.
- **`bind.rebind_exec` could raise despite documenting that it can't** — a corrupt `direct_url.json` propagated out of `relaunch._editable_path` and killed the MCP server at startup, rather than degrading to an unbound kernel. All the metadata reads are guarded now.
- **`repld log --json` emitted a non-JSON line into the stream**, so `| jq` broke precisely when no kernel was running, which is when you are most likely reading the log. The notice moved to stderr.
- **`repld gist list` ignored its arguments** — `-h` printed the listing and an unknown flag was silently accepted. Every other `repld gist` verb checks both.
- **`browser.open()` failed on any URL the caller had also watched.** The new target matched the pattern, so an auto-attach claimed it first and `session.attach()` correctly returned `None` — which `open()` treated as fatal instead of waiting for the attach already in flight, as `get()` does.
- **A gist using `from __future__ import annotations` got a wrong tool schema.** Under PEP 563 every annotation reaches `inspect.signature` as a string, so `int` stopped mapping to `integer` and `Annotated[T, "..."]` was invisible — silently typing every parameter `string`. Schemas resolve through `typing.get_type_hints(..., include_extras=True)` now.
- **Adopting a late-appearing venv sent a malformed channel push.** `push_kind(content, kind)` was called with its arguments swapped; both parameters are `str`, so it type-checked, linted and ran. A `push_kind` arg-order sweep now runs in phase 4 across every call site in `src/`.
- **Answering one gate could strand another.** The pane tracked a single open gate while `gates` has always allowed several, so answering an older one out-of-band left the gate actually on screen unanswerable from stdin.
- **`repld status` could warn about a virtualenv belonging to neither party** — `bind.project_venv()`'s `$VIRTUAL_ENV` fallback ignored the cwd it was given, so a status run from an activated venv judged an unrelated kernel against it. It grew an `ambient=False` for callers asking about another process's project.
- **`repld restart` reported failure while a healthy kernel was coming up.** `spawn_headless` returned `False` both for a failed spawn and for a racing boot that already owns the systemd unit; it returns `STARTED` / `INCUMBENT` / `FAILED` now, so only the last is terminal.
- **`choose()` answered by number returned the number.** The prompt renders options as `1=alpha, 2=beta`, so typing an index is the obvious move — and the stdin reader passed the line through verbatim. Coercion lives in one place now (`gates.parse_response`), shared by the pane and `repld gate answer`.
- **The loop watchdog reported a kill it hadn't performed.** It cancels via `loop.call_soon_threadsafe` — a callback on the very loop it just declared wedged — so `killed blocked task` told the agent a task was gone while it might run for minutes. It reports the cancellation as requested now, effective when the loop unblocks.
- **One unreadable session file could take down `repld status` and `repld stop --all`.** `list_sessions()` falls back to the filename pid when contents don't parse, but returned the unparseable payload anyway, and every consumer indexes `pid` directly — so the command you reach for when something is wrong crashed with a `KeyError`. Only the unusable payload is dropped now.
- **A long-lived kernel never reclaimed its own scratch.** Task-spill eviction walks the *task registry* and the boot sweep only collects what a *dead* pid left behind — neither of which helps a process designed to stay up for weeks — so a live kernel's spills, oversized `resources/read` and browser tool responses, and every screenshot accumulated in tmpfs for its whole life. Both classes age out at the same one-hour bound now, and the prune pass is driven by the loop watchdog rather than every 50th `finalize()`, which never arrives on an idle kernel. **A path handed to the agent in a tool response, `[full output: …]` included, stays readable for roughly an hour.**
- **A hung dependency install could wedge the whole kernel** — `install_deps()` ran with no timeout, and two of its three call sites are in the gist import hook, i.e. on the shared asyncio loop inside the `exec` that triggered the import. Installs are capped at 10 minutes now, and a timeout prints the command to run by hand.
- **A dead-but-unreaped kernel counted as running.** `os.kill(pid, 0)` succeeds for a zombie, and repld makes those routinely since the bridge never waits on the kernel it spawns — so `repld stop` reported "did not exit within 10s" about a process that had exited, and stale lockfiles were blessed. `pid_alive()` checks `/proc/<pid>/stat` for state `Z`, degrading to its previous behaviour where there is no procfs.
- **`repld log` printed cell output one *chunk* per line instead of one line per line.** Output reaches the event log as a byte stream, so a single `print("a", b)` arrives as four chunks; it is written verbatim now, the way the TUI always did.
- **A request could hang forever if the kernel died at exactly the wrong moment** — the socket could go to `None` between the liveness probe and the write, returning with the id already registered and nothing left to answer it. It goes through the same orphan path a failed send does, answering `-31001`.
- **`repld gist lint` reported three things that were not wrong, and described a fourth rule more strictly than it enforces it.** The `deps` rule flagged the soft-dependency idiom (`try: import x / except ImportError`) and compared import roots against *distribution* names, so a gist declaring `pillow` was told it hadn't declared `PIL` — the linter and the installer disagreeing about the same declaration. Declarations now resolve to the import roots they can satisfy (settling the `"."` self-dep and `_`-prefixed siblings too), and the `shape` rule's message asks for what it actually checks.
- **Gist dependencies now install somewhere durable, stay visible to a bound kernel, and work under a system Python.** They went into whichever venv was active, gated on a `"uv/tools/" in sys.prefix` test that binding made false — so the accumulated deps became invisible and fresh installs landed in an ephemeral `uv run` overlay, returning the prompt forever. Installs use `uv pip install --target` into a shared directory keyed by interpreter version, *appended* to `sys.path` so a gist can't shadow the project's own resolution. **The loose files directly under `~/.local/share/repld/deps/` are unused now** and can be deleted by hand; expect one install prompt per project on the first boot with a terminal.
- **`repld exec` is no longer slowed by the venv bind** (~336 ms → ~82 ms). It resolves a lock path and talks IPC, and the code it runs executes in the kernel, which is already bound; only `repld`, `repld bridge`, and `repld restart` bind now.
- **A kernel that failed to start printed nothing at all.** `install_tee()` runs partway through boot and never touches the underlying stream, so anything failing after it reached only the event log with no display thread alive to render it — an over-long `--socket` path, a permissions problem, and an already-bound port all presented as a bare non-zero exit. Boot writes `repld: kernel failed to start` plus the traceback to the real stderr now.
- **Private gists were skipped by dependency scanning and by `repld gist lint`, but only when local.** An underscore-prefixed file isn't a gist but *is* importable, and `_iter_gist_files()` filtered privates from the installed-directory branch and not the linked one — so a private helper's `__repld_deps__` was never scanned at home and the sibling importing it died on a bare `ModuleNotFoundError`. Scanning and lint see privates wherever they live now; listings and tool discovery still never do. **A private gist declaring `__repld_deps__` is scanned at boot, and prompts where there is a terminal.**
- **Per-process scratch left by dead kernels is now swept at boot.** Nothing runs on the way out of a SIGKILL and no later kernel looked, so task spills, resource spills, and screenshots accumulated — thousands of files, in tmpfs under a real `$XDG_RUNTIME_DIR` and permanently under the `/tmp/repld-{uid}` fallback. Every such file is named `{pid}-…`, so a booting kernel reclaims a dead pid's leftovers in one pass and can never touch a running kernel's output.
- **`repld log` and the live TUI render the same events identically again.** Three formats had drifted — the human-gate response, the tab title on browser attach lines, and a channel push leaving colour switched on after the top border. They come from one module now.
- A bridge whose first kernel spawn timed out replayed the client's `initialize` onto the kernel it then reached, emitting `tools/list_changed` before the client's own `initialize` had been answered. The handshake is cached only once it is on its way to a kernel, so the first is never mistaken for a replacement.
- `repld help`'s state check stat'd `./.pyrepl.lock` and advised deleting a stale one by hand — a file that no longer exists, and advice the flock mutex made wrong anyway.
- `repld stop --all` aborted the whole sweep with a `ValueError` if any session file lacked a `socket_path`, instead of skipping that entry.
- `repld exec`'s interactive banner listed every injected builtin except `no_display` — the one whose whole purpose is to be discovered at the REPL.
- The dashboard hint file was only ever written by browser code, so on a kernel that never touched the browser `repld status` couldn't authenticate to the dashboard API and the dashboard drew a fresh random port on every restart. It is written as soon as the server binds, and merges rather than replaces so a headless boot can't discard the previous kernel's restorable Chrome session.

### Removed

- **`repld init` is gone, and repld no longer writes anything into a project.** Both files it wrote were snapshots of advice frozen at the moment it ran, and the `.mcp.json` proved the point by drifting when `bind.rebind_exec` moved binding inside repld — leaving every earlier project on `uv run repld bridge`, paying a per-session `uv sync` to reach the same interpreter. The CLAUDE.md block was redundant besides: every claim in it is already in the MCP `initialize` instructions, which are always loaded and always current. See Upgrading above.
- **`repld --init FILE` is gone, replaced by an auto-detected `repld_init.py`.** The flag only ever fired for a human starting the kernel by hand *and* winning the flock, and `spawn._kernel_argv` records no init path — so a project whose kernel *hosts* something lost its entire bootstrap, silently, the moment a lazy spawn or restart replaced it. Both failure modes are gone rather than fixed, there being no per-invocation flag left to drop.
- **The `__repld_tools__` tool-registration list is gone**, a release earlier than planned: a scan of every registered gist found three users, all migrated first. A file still carrying one is ignored rather than warned about, so `repld gist lint`'s `legacy` rule is now the only thing that reports it. Two things get better as a result — a tool taking a single `dict` argument works now (it was indistinguishable from the old `handler(args)` shape and silently skipped), and a file mixing both conventions no longer has its typed functions suppressed.
- **`repld[browser]` now requires Chrome 140+.** `Tab.fetch()`'s hand-chunked `btoa` fallback for browsers without `Uint8Array.prototype.toBase64` is gone; that method has been on by default since Chrome 140 and Baseline since September 2025, and only binary response bodies ever touched the path.
- Dead compatibility branches and `.gitignore` scaffolding: `repld init` no longer writes `.pyrepl.*` ignore entries, since nothing repld writes at runtime lands in the project directory. Existing lines are harmless and can be deleted.

## [0.1.1] - 2026-07-24

### Added

- `repld gist lint [name...]` — AST-based best-practices checker for gist files. Four rules: `firstline` (module docstring's first line must stand alone — it's what gets truncated into tool listings/instructions/resource descriptions), `shape` (public methods returning dict/list/Any should document the return shape on the docstring's first line), `deps` (non-stdlib imports should be declared in `__repld_deps__`), `legacy` (flags `__repld_tools__` statically instead of only warning reactively at tool-call time). Suppress a finding with `# gistlint: ignore=<rule>` on the flagged line or the line above. Scans the same dirs + precedence a live kernel resolves against (`~/.repld/gists`, `./gists`, and `.links`), not just the local project. Documented in `repld://docs/guide`'s Conventions section, `repld help gists`, and `repld help`'s command list
- `__repld_deps__` gained a `"path:some/dir"` form — prepends a local, non-pip-installable directory to `sys.path` (e.g. a vendored git submodule), relative to the gist's project root. No install step; a missing directory warns loudly with a `git submodule update --init` hint instead of a bare `ModuleNotFoundError` the first time the gist runs. `gist lint`'s `deps` rule treats top-level modules/packages found in the resolved directory as declared, so imports from it don't false-positive. Modules imported from a path dep get the same mtime-based auto-reload as gists — `_GistFinder` tracks them too, flagged separately so editing vendored code re-imports the new version without a kernel restart, but without triggering the gist-registry/API-summary side effects that don't apply to third-party code

### Changed

- `gists.import_hint()` is now a shared helper — `build_instructions()` (always-loaded MCP instructions) and `introspect()` (the on-demand `repld://gists/{name}` resource) both call it instead of each computing their own import line, so they can't show different (or no) import advice for the same gist
- `browser_screenshot` tool description now warns that reapplying `Emulation.setDeviceMetricsOverride` on an already-emulated tab can leave viewport metrics inconsistent — recommend a fresh tab per distinct size and verifying `clientWidth === innerWidth` before trusting the capture
- `BROWSER_GUIDE` (`repld help browser`) gained a "Mobile viewport testing" section: CDP emulation pitfalls and the ADB-forwarded real-device fallback (`adb forward` + `Browser(port=)`)
- Another round of internal cleanup: `row.py`'s HAR/console/SSE/lifecycle factory functions now index SQL results by column name instead of raw tuple position (a reordered column in `har.py` used to silently corrupt fields, caught only by a hand-maintained comment); `kernel.py`/`gists.py` stopped reaching into `tasks._current_task`/`gist_links._linked` directly in favor of `tasks.set_current_task()`/`gist_links.linked_names()`/`linked_path()`/`linked_items()`; `gists.py`'s `scan_tools`/`resolve_tool` share one `_try_import_gist` helper; `kernel.py`'s `@every` ticker uses the `_push()` helper consistently; `gist_cmd.py`'s `new`/`add`/`rm` usage messages are now consistent on empty args; dashboard's unused `state["browser"]["available"]` field was dropped
- Codebase health pass: `row.py`'s query factories now take dicts keyed by the query's *live* column names (new `CDPSession.query_dicts()`, sourced straight from the DuckDB cursor description) instead of hand-maintained column-name tuples zipped positionally against `SELECT *` — closes the gap where a column reorder in `har.py`'s views could still silently misassign fields despite the previous length-only `assert`. `protocol.py`'s `Dispatcher` split further: the ~300-line `browser_*` tool dispatch moved to a new `browser_dispatch.py` (`BrowserDispatchMixin`), `tab.py`'s DuckDB-backed query methods (`network`/`console`/`sse`/`lifecycle`/`body`/`request`) moved to a new `browser/tab_query.py` (`TabQueryMixin`), and `KernelContext` moved to its own `kernel_context.py` so both dispatcher halves can depend on it without a circular import. `help.py`'s `BROWSER_GUIDE` and `repld help browser` no longer restate the same pin/gate-bridge and `__repld_deps__` prose in two places; the module docstring's "no overlap" claim now describes the actual terse-topics/prose-resources split. `gist_cmd.py`'s private-file filter and per-subcommand `-h`/`--help` checks are shared helpers instead of copy-pasted; `gists._linked_path`'s one-line forward to `gist_links.linked_path()` is inlined; `browser/observe.py`'s `TYPE_CHECKING`-guarded `Tab`/`BrowserSession` imports (no actual import cycle exists) are now real imports; the kernel lockfile's `started` field is renamed to `started_at` to match the session registry's field name

### Fixed

- Console-error and dashboard-connect channel notifications built short target IDs (e.g. `9222:5CA9D0`) from the raw CDP `targetId` without lowercasing, unlike the canonical `make_target()` helper — an ID copied straight from a notification would fail every `browser_*` tool call, since `_is_target_id()`'s classifier regex only matched lowercase hex and silently misrouted the uppercase ID as a URL glob instead of a target ID (surfacing a confusing "no tab matching" error rather than something actionable). `_push_error_text` and the dashboard's connect summary now lowercase consistently with `make_target()`; `_is_target_id()`'s regex is case-insensitive and `_split_target()` normalizes its prefix, so any uppercase ID — from any source — now resolves instead of just being masked at the two known origins
- `install_deps()`'s tool-venv path (`uv pip install --target ~/.local/share/repld/deps`) resolved each newly-missing dep in isolation, blind to everything already installed in that shared, cross-project directory — two gists whose transitive deps disagreed (e.g. one pinning `pydantic==2.13.4`, needing `pydantic-core==2.46.4`, while a later install pulled a newer `pydantic-core` in unrelated to that constraint) could silently leave mutually-incompatible versions on disk. `gist_deps.py` now maintains a `.repld-manifest.txt` of every requirement ever installed there and re-resolves the full accumulated set on each install, not just the new arrivals
- `Tab._wait_for_node` cached the root `DOM.getDocument` nodeId once before its poll loop; a document replaced mid-wait (SPA navigation, HMR reload) left it stale, so `click()`/`type_text()`/`wait_for()` silently never matched and raised a misleading "Element not found" instead of surfacing the navigation race. Root nodeId is now re-fetched every poll iteration
- `gists.resolve_tool` could dispatch to a typed `_tool_*` function in a gist file that also declared unrelated legacy `__repld_tools__` entries — a case `scan_tools`/`tools/list` never advertised, since a legacy-declaring file suppresses all typed tools in that file. Both now share one `_declared_tools` classifier, so listing and dispatch agree
- `Tab._setup_binding` called `self._session.execute()` directly instead of `self._exec()`, skipping the session-gone recovery every other CDP call gets — pin setup could raise instead of self-healing across an HMR reload or navigation race
- `Tab.sse()` and `Tab.lifecycle()` ordered results by `ORDER BY rowid`, a column their underlying views never expose (only `id`) — both raised a DuckDB `BinderException` on every call
- Dashboard's per-pattern tab-count display recomputed glob matching in JS with a hand-rolled regex that always escaped `[`/`]`, diverging from the server's `fnmatch`-based watch matching — bracket-class patterns could show a stale count. Counts are now computed server-side in `BrowserPool.snapshot()` next to the actual match logic, and the client just renders them
- `gists._resolve_json_type` didn't unwrap parameterized generics (`list[str]`, `dict[str, int]`) when inferring an MCP tool schema from a `_tool_*` signature — fell through to the "unmapped type" warning and silently schema'd the param as `"string"`
- `gists.signature_for_path` (and `signature()`) picked a gist's *first* public class by file order for the "here's how to import this" hint, with no regard for whether it was the entry point — a custom exception type declared before the main class (a common pattern, e.g. `GigahostError(RuntimeError)` before `Gigahost`) would win instead, producing a hint that imported the wrong class. Now skips classes whose name or base ends in `Error`/`Exception`
- `gist_deps.scan_deps()` only ever ran once, at kernel boot — adding `__repld_deps__` entries to a gist file already imported by a long-running kernel never got checked or prompted again; you'd have to know to restart the whole kernel process to get the install prompt. `gists._check_reload()` now re-scans just the changed file's deps on every auto-reload, so a newly-declared dependency gets caught the next time the gist is (re-)imported instead of silently waiting for a restart. Also deduped `_GistImportHook`'s `base`/`top` reload checks (equal for any flat, non-package gist) so the new prompt can't double-fire for one reload
- `gist_deps.install_deps()` never pinned `--python` when installing into `_TOOL_DEPS_DIR` (the side-install dir used when repld runs as a global `uv tool install`, unlike the project-venv branch right below it, which already did). `uv pip install --target` with no `--python` resolves against uv's own default interpreter, which can silently differ from `sys.executable` — a binary wheel (`cffi`, `pydantic-core`, `rpds-py`, ...) built for the wrong Python ABI lands in the shared deps dir and fails with a bare `ModuleNotFoundError` for its compiled extension deep inside whatever imported it, nowhere near the actual cause. Now pins `--python sys.executable` in both branches
- A gist file created *after* the kernel had already booted skipped its `__repld_deps__` scan entirely on first import — `_GistImportHook`'s pre-import `_check_reload()` call is a no-op for a module `_GistFinder` has never resolved before (nothing in `_managed` to compare mtimes against yet), so only the boot-time scan (everything present at start) or a later edit (`_check_reload`'s own rescan) ever triggered it. "Write a new gist, `import` it right away" — an entirely normal same-session workflow — silently skipped the check, surfacing missing deps as a bare `ModuleNotFoundError` instead of an install prompt. `_GistFinder.find_spec` now runs the same scan the first time it tracks a `fullname`, across all three of its resolution tiers (local gist dirs, cross-project links, `path:` dep dirs)
- `gist_deps._is_importable()` only ever tried a dep's declared name verbatim (modulo `-`→`_`), so any package whose importable module name differs from its PyPI distribution name (`pyyaml`→`yaml`, `beautifulsoup4`→`bs4`, ...) was reported "missing" on *every* scan forever, install or not — the install itself succeeded harmlessly each time, but the kernel re-prompted on every boot and every first import indefinitely. Now falls back to `importlib.metadata.packages_distributions()`'s reverse mapping (distribution name → actual top-level import names) before giving up; the cache is dropped after `install_deps()` installs something so a same-session install is picked up immediately
- Kernel shutdown blocked up to 5 real seconds in `_browser_cleanup` whenever a browser had ever been attached: its atexit hook scheduled `real.disconnect()` on the kernel's asyncio loop via `run_coroutine_threadsafe`, but `_shutdown()` always stops that loop *before* atexit runs, so the coroutine had nothing left to drive it forward and the wait just ran out its 5s timeout every time. Because `_cleanup_lockfile`'s atexit hook (LIFO-ordered) runs *before* `_browser_cleanup`'s in practice, `.pyrepl.lock` was already deleted up to 5s before the process actually exited — a real window for a fresh `repld` invocation to start while the old kernel (and its IPC sessions) were still alive. `_browser_cleanup` now skips the coroutine dispatch entirely when the loop isn't running. Related: `ipc.Session._close_locked()` closed the raw socket but left its `rfile`/`wfile` `TextIOWrapper`s for GC/interpreter-shutdown finalization to close, which surfaced as an uncatchable `BrokenPipeError` ("Exception ignored in...") when their buffered flush hit the already-dead socket — both are now closed explicitly, with the same errors caught deliberately instead
- `Tab._reattach()` only recovers from CDP *session* invalidation (same target, new session — e.g. HMR reload); when a target is destroyed outright and replaced (a genuine cross-origin/site-isolation process swap), it still re-attaches by the old target id, so `Target.attachToTarget`'s raw CDP error ("No target with given id found") propagated straight out of `_exec()` uncaught, unexplained. Now catches that failure and raises a clear error pointing at `browser_tabs` to find the replacement, instead of passing Chrome's raw text through unchanged
- `repld/__init__.py`'s `TYPE_CHECKING` block stubbed `notify`/`defer`/`every`/etc. for `import repld; repld.notify(...)` gist patterns but never added `browser`, despite `kernel.py` genuinely injecting it into the module at runtime — every gist referencing `repld.browser` type-checked as an attribute-access error


## [0.1.0] - 2026-07-14

### Security

- Dashboard `POST /api` sent `Access-Control-Allow-Origin: *`, letting any webpage open in any local browser drive `browser.connect`/`watch` or read captured network/console data (auth headers, cookies) via CSRF/DNS-rebinding. Now requires a random per-boot bearer token (embedded in the served dashboard page) and echoes CORS headers only for the dashboard's own origin
- The dashboard bearer token was still readable via DNS rebinding: a page on an attacker domain resolving to 127.0.0.1 is same-origin in the browser's eyes, so it could fetch `GET /` (which embeds the token) and then drive `POST /api`. All dashboard requests now require a loopback `Host` header (`127.0.0.1:<port>` / `localhost:<port>`) and 403 otherwise

### Added

- Type-hint gist tool registration: `_tool_*` functions with type hints are auto-discovered as MCP tools — schema inferred from parameter annotations/defaults and the first docstring line. Replaces the two-piece `__repld_tools__` + `_tool_*(args: dict)` convention with a single typed function declaration
- Session registry: every kernel writes `$XDG_RUNTIME_DIR/repld/sessions/<pid>.json` on boot and removes it on shutdown, so any repld instance (or its dashboard) can enumerate live siblings. `repld.sessions.list_sessions()` prunes dead PIDs lazily
- Graceful browser disconnect: `browser.disconnect(port=)` now unpins tabs (removes pill + beforeunload guard + heartbeat) before closing the WebSocket, and returns a summary string. `browser_detach` MCP tool gains `target` (detach one tab) and `port` (disconnect a whole Chrome instance) params alongside the existing `pattern`
- Dashboard sidebar: left rail listing all live repld sessions (project name, uptime, status dot), with the current session highlighted and siblings linking to their own dashboard. New "Connections" tab shows per-port browser connections, expandable to individual targets, with disconnect/detach buttons
- `no_display(value)` builtin: return a value from a cell without the auto-display hook re-printing it, while still binding `_`/`_N` and surviving direct assignment (`x = await foo()`) for programmatic use — for functions that already print their own output
- `repld browser` subcommand: re-execs `repld` under `uv run` with the `browser` extra (`duckdb`+`websockets`), so browser tools work in any project without adding `repld-tool` as a dependency. Detects and preserves a local editable checkout (via `direct_url.json` distribution metadata) instead of silently swapping to the published package
- `tab.scroll(selector, dy=0, dx=0)`: touch-scroll a container by pixel offset — sugar over `swipe()` that resolves the selector to its center and swipes the opposite direction (scrollBy semantics: positive `dy` scrolls down, positive `dx` scrolls right)

### Changed

- `Browser.from_profile(path)` replaced by `browser.connect(profile=path)` — the classmethod returned a bare `Browser` outside the pool, so MCP tools and the dashboard never saw its tabs; the pool form resolves the port from `DevToolsActivePort` and connects normally
- `gists.py` split: dependency management (`scan_deps`/`install_deps`) now lives in `gist_deps.py`, cross-project links (`add_link`/`remove_link`/`link_targets`/manifest handling) in `gist_links.py` — import machinery, AST introspection, tool scanning, and the registry stay in `gists.py`
- `ask()` now accepts `tab=` like `confirm()`/`choose()` — routed for symmetry, though the pill UI has no text input so the response is still typed in the terminal (previously passing `tab=` raised TypeError)
- `tab.label` (colored identification bar, survives navigation) is now documented in `repld help browser` and the browser guide
- `BrowserSession.attach()` accepts the caller's target-info dict and skips a redundant `Target.getTargets` round-trip — `watch()` on N matching tabs no longer issues N full target listings
- Gist AST parsing is memoized on file mtime — a single MCP `initialize` previously re-parsed every gist file four times (docstring scan, signature, usage, tools)
- `repld exec` consumes every `--socket` flag occurrence (first value wins) instead of only stripping the first `--socket PATH` pair
- Gist registry writes are skipped after the first import of a given gist per kernel process (previously a full read-parse-write of the registry JSON on every re-import)
- Importing `repld.display` no longer asserts on `sys.__stdout__`/`sys.__stdin__` at import time, so `--no-display` works in stdio-less environments; display mode checks at startup instead
- `browser_screenshot`'s resize step now uses Pillow instead of a hand-rolled PNG decoder (new `pillow` dependency in the `browser` extra) — handles every PNG variant Chrome can emit and runs in a thread executor instead of blocking the kernel's shared asyncio loop for the ~150-200ms a resize takes on realistic screenshot sizes
- `repld gist new` template scaffolds the new typed `_tool_*` pattern (no `__repld_tools__`)
- `__repld_tools__` still works as a legacy override for custom schemas, but prints a one-time-per-gist deprecation warning at boot
- Kernel boot no longer silently reconnects Chrome ports and re-watches tab patterns from the previous session's dashboard hint — it now prompts on the terminal (`[Y/n]`, default yes) before restoring. Headless boot (`--no-display`) or non-tty stdin skips the restore entirely rather than blocking on a prompt no one can answer
- Browser tab pin state (`_pinned`, `_pin_reason`, `_pin_origin`, `_heartbeat_task`) now lives on `CDPSession` instead of `Tab` — `Tab` wrappers are recreated on every `get()`/`_iter_tabs()` call, so pin state used to reset whenever a tab was re-fetched. `CDPSession` persists for the life of the attachment, matching the existing pattern for `capture_bodies`
- `Tab.fetch()` / `browser_fetch`: string request bodies now default to `Content-Type: application/x-www-form-urlencoded` (dict bodies unchanged, still `application/json`); caller `headers` override, matched case-insensitively
- `BrowserPool.get()`'s cross-browser retry loop caught bare `RuntimeError`, which could mask a real CDP/ready-signal/reattach failure as "try the next browser" instead of surfacing it. A dedicated `TabNotFoundError` now carries the "no such tab" case specifically, and the retry only catches that
- Several rounds of internal cleanup from codebase health passes: kernel boot split into phase helpers; duplicated ready-signal polling, `__repld_usage__` extraction, AST/warn-once parsing (`__repld_tools__`/`__repld_deps__`), target-ID parsing, tool-venv `sys.path` setup, attach+enable_fetch+wrap, `push_channel` call sites, dashboard `_bound_port`, terminal-bypass writes, heartbeat constants, `INSERT INTO` event-store statements, and doc-resource lookups each consolidated into one helper/constant; `tasks.snapshot()`/`kernel.preview_since()`/`gists._read_registry()` added as proper public seams instead of reaching into another module's private state; `_gate()` gained per-kind overloads instead of a `type: ignore`; assorted doc drift fixed (`browser.patterns()` being a property, `browser_key`'s description, selector/deps docs, GUIDE's builtins recap documented as the sanctioned doc-overlap exception)

### Fixed

- Cross-process JSON files (lockfile, dashboard hint, session registry entries, gist registry, link manifest) were written with plain `write_text`, so a crash mid-write could leave a truncated file for concurrent readers (the bridge and `repld exec` read the lockfile/hint on every connect). All five now go through one tmp-then-`os.replace` helper; the dashboard hint's `0600` mode also lands before the file becomes visible instead of just after
- Dev-checkout kernels prepended the tool-venv gist-deps dir (`~/.local/share/repld/deps`) to `sys.path`, shadowing the project venv's site-packages with extension modules built for a different interpreter — a stale Pillow there silently disabled the `browser` builtin at boot. The dir is now only added when actually running from the uv tool venv, matching `install_deps`'s gating
- `browser.connect()` with no port ignored `REPLD_CHROME_PORT` and always defaulted to 9222; it now honors the env var like the rest of the browser layer
- The per-tab DuckDB event store was read (`tab.network()`/`console()`/`body()` MCP tools) from IPC reader threads while the kernel loop wrote events through the same connection object — DuckDB connections aren't thread-safe. Reads and `clear()` now run on a per-call cursor (duplicated connection, DuckDB's sanctioned cross-thread pattern)
- `get_task` with an unknown `task_id` returned a *successful* response whose `_meta` carried only `{task_id, error}` — no `done`/`text` fields — instead of a JSON-RPC error like `cancel` does for bad input
- `repld help browser` listed `tab.controls()` and `tab.invoke()` under the sync DuckDB-query heading; both are async (calling them bare returns a coroutine). The browser guide's `controls()` entry also didn't say it's async
- `tab.capture_bodies` / `tab.label` setters fell back to the deprecated `asyncio.get_event_loop()` when the session had no loop — now raise a clear `RuntimeError` instead (the fallback was unreachable in practice; the loop is always set at attach)
- `__repld_deps__` requirements with extras (e.g. `httpx[http2]>=0.27`) were falsely flagged as missing on every boot — the parsed package name kept the `[...]` suffix, which `importlib.util.find_spec` can never resolve
- Cell execution counter (`_N`/`_` history) could race and hand out duplicate numbers when two MCP sessions called `exec` concurrently, since the increment wasn't atomic across IPC reader threads
- `repld gist add <name>` failed for gists that are only ever invoked as MCP tools (never `import`ed by user code) — they were never written to the cross-project gist registry
- `browser_screenshot` silently returned the untouched, full-size PNG (with no error) for any screenshot that wasn't 8-bit RGB/RGBA — while still reporting the *intended* downscaled dimensions in the response metadata, so callers had no way to tell the image didn't actually match what was reported
- `Tab.sse()` / `Tab.lifecycle()` queried with `LIMIT 500` but no `ORDER BY`, while their backing views (`sse_entries`, `lifecycle_entries`) are defined oldest-first. Once a session passed 500 SSE messages or lifecycle events, these methods silently returned the *oldest* entries instead of the most recent — the opposite of every sibling query method (`network()`, `console()`)
- Browser doc drift: the `repld help browser` topic documented `tab.type_text(..., enter=)` (the real parameter is `press_enter=`) and neither it nor `docs/browser` listed `tab.key()`; the `confirm()`/`choose()` type stubs were missing the `tab=` parameter that routes gates to a pinned tab's pill UI
- `BrowserSession.connect()` fetched `/json/version` with a synchronous `urllib.request.urlopen(timeout=5)` on the kernel's shared asyncio loop — every connect *and* auto-reconnect could stall the whole kernel for up to 5s when Chrome was slow or unreachable. Now runs in a thread via `asyncio.to_thread`
- `tab.body()` / `row.body()` called from `exec` code (which runs *on* the kernel loop) deadlocked for 10s and then errored whenever the body wasn't in the capture store — the sync CDP fallback blocked the very loop it needed to run its coroutine on. Now fails fast with guidance to use `await tab.cdp('Network.getResponseBody', ...)` instead; the thread-side (MCP tool) path is unchanged
- `browser_screenshot` wrote the PNG to disk synchronously on the kernel loop; the write now runs in a thread executor like the resize step
- Tab label state (`tab.label`) lived on the ephemeral `Tab` wrapper, so re-labelling a re-fetched tab orphaned the previous label's injected script and DOM bar instead of replacing it. Label state now lives on `CDPSession`, matching pin state
- Re-attach with a CSS `ready=` selector always timed out: the ready poll held a `DOM.getDocument` nodeId from before the page settled, which goes stale when the document is replaced mid-load and then silently never matches. The poll now evaluates `document.querySelector(...)` via `Runtime.evaluate`
- `_parse_pkg_name` split multi-clause requirements (`foo>=1.0,!=1.2`) at whichever version specifier set iteration found first, sometimes producing a mangled package name and a spurious install prompt. Now splits at the earliest-occurring specifier
- Channel-kinds doc in `repld help exec` was missing `pin_lost` and `browser_disconnect`
- A gist declaring a dep whose dotted parent package is missing (e.g. `__repld_deps__ = ["ruamel.yaml"]`) crashed kernel boot — `importlib.util.find_spec` raises `ModuleNotFoundError` for such names instead of returning `None`; now treated as "not importable"
- `resources/read` truncated every resource to the 4KB exec-output preview, so `repld://docs/*` never returned the actual docs to MCP clients (only a head/tail preview plus a local spill path). Resources now return full text up to 64KB — only the unbounded browser network/console dumps can still spill — and honor each resource's declared mimeType (`repld://browser/controls` was declared `application/json` but always served `text/plain`)
- The dashboard's per-tab Detach button never worked: `BrowserPool.snapshot()` built target IDs without lowercasing while the detach lookup compares lowercased, so every click returned "Target not found"
- HMR/navigation re-attach built a brand-new CDP session, silently discarding the tab's event history, body-capture state, and pin/label state, and leaking the old session's DuckDB connection. Re-attach now preserves the session in place (same mechanism as Chrome-restart reconnect) and re-registers the label script
- `tap`/`swipe` and `browser_tree` bypassed the session-gone recovery wrapper, so touch input and tree reads failed permanently after an HMR reload while clicks recovered
- Every browser mutation tool polled a heavyweight DuckDB HAR view at 20Hz *synchronously on the kernel loop* while waiting for network idle; settle now reads an in-memory in-flight counter maintained on the event path
- `repld gist new -h` / `add -h` / `rm -h` treated `-h` as a gist name (or exited non-zero); they now print usage and exit 0
- Scaffolded gists (`repld gist new`) shipped an unused `import os` that lint flags in the user's project
- Doc drift: `repld help browser` claimed CSS selectors use `DOM.getBoxModel` (the code uses `DOM.getContentQuads`) and understated `ready=` as CSS-only (it also accepts JS expressions)
- Missing `task_id` on `get_task`/`cancel` returned a generic internal error (`-32603 KeyError`) instead of the proper invalid-params error (`-32602`)
- Multi-line `str` cell results print verbatim instead of being `repr()`-escaped into an unreadable single line with literal `\n`s
- `repld://gists/{name}` signature listings no longer render `@property`/`@cached_property` methods with call parens (was misleading agents into calling e.g. `.pid()` and getting a `TypeError`)
- `browser_fetch` / `Tab.fetch()` silently sent string bodies with no `Content-Type`, so the browser defaulted to `text/plain` and form-decoding servers saw an empty form — root cause was in `Tab.fetch()`'s header defaulting, not the MCP transport
- `browser_fetch` tool schema: `body` had no declared `type`, so MCP clients could silently flatten a dict argument to a JSON string instead of sending it as an object. Now typed `["object", "string"]`, matching what the handler actually accepts
- `no_display(value)` only unwrapped on a cell's bare last expression — direct assignment (`x = await foo()`) left `x` bound to the internal wrapper object instead of the real value, contradicting its own "still returning it... for programmatic use" contract. `compile_cell()` now also collects top-level simple-assignment targets (`x = ...`, chained `x = y = ...`, annotated `x: T = ...`) and `run_cell()` unwraps `_NoDisplay` off them after they're bound. Doesn't cover tuple/list/starred/attribute/subscript targets or walrus expressions nested in larger expressions
- `browser.watch(pattern)` silently reported "Attached 0 new tab(s)" when a matching target existed but the CDP `Target.attachToTarget` call failed (e.g. another debugger already attached to it) — the exception was swallowed and logged only at `DEBUG` (no logging is configured anywhere in the package, so it was never actually emitted). The returned summary now appends `N attach attempt(s) failed: <target>: <reason>` when any attach errors, instead of looking identical to "nothing matched the pattern"
- `BrowserJSError.stack` was always a copy of `.text` (never an actual stack trace), contradicting the docs' claim of a "preserved stack trace". Now parses CDP `exceptionDetails.stackTrace.callFrames` into a real multi-frame string
- Kernel boot swallowed dashboard-start, Chrome-port-reconnect, and pattern-re-watch failures with a bare `except: pass` — a broken dashboard import or failed session restore was invisible. Now logged to stderr
- `repld exec` had no `--socket` flag despite `repld bridge` supporting one, so pointing `exec` at a kernel on a non-default socket path required setting `REPLD_SOCKET` instead
- `Tab._wait_ready()` silently no-op'd on navigate/reload whenever `ready=` wasn't explicitly passed (the common case) — `self._ready` was `None` so the method returned immediately, skipping even the default `document.readyState === 'complete'` wait. Now falls back to that expression instead of returning early
- `Browser.detach(pattern=...)` (and detach-all) never unpinned tabs before detaching, unlike `detach_target()` — a pinned tab's pill, beforeunload guard, and heartbeat task leaked whenever it was detached via a pattern or `detach()` with no argument
- `resolve_gate()` could raise (or silently do nothing, depending on timing) when the terminal and a pinned tab's pill raced to resolve the same human-gate — `Future.set_result()` on an already-resolved future raises `InvalidStateError`, which wasn't caught. First responder now always wins cleanly
- Gist registry writes (`_register()`) crashed on a corrupt `gist-registry.json` instead of resetting it, unlike the read path (`registry()`); both now handle corruption the same way
- `_every_registry` (the set of active `@every` tickers) was mutated from the asyncio loop thread, sync-cell threads, and the dashboard's HTTP thread with no lock — the same missing-lock bug class as the task registry, just not yet hit in practice
- `_tasks` (the task registry) had no lock guarding it against concurrent watchdog-thread/IPC-reader-thread access; entries were also never evicted after completion, leaking forever in a long-running kernel. Now locked and evicted 1h after `done_event`
- `BrowserPool.get(target, timeout=N)` gave each connected Chrome instance the *full* timeout before trying the next one, so with N connected browsers a "not found" could take up to N×timeout instead of timeout total. Now shares one deadline across all browsers
- `Tab.capture_bodies` setter flipped the visible flag before `enable_fetch()`/`disable_fetch()` had actually finished (or even started, since the CDP call is fire-and-forget from a sync setter) — code reading `capture_bodies` right after setting it could see the new value while Fetch interception was still in its old state
- `_pick_victim` (the loop-watchdog's kill-candidate scan) iterated `tasks._tasks.values()` directly instead of a snapshot, risking a `RuntimeError: dictionary changed size during iteration` if a task was added or finalized mid-scan
- `hint_for_name()` (the "did you mean to import this gist?" suggestion) only scanned `_installed_dirs`, so it never suggested a gist that was only present via cross-project linking (`repld gist add`)
- `observe.py`'s iframe-tree attachment matched on a substring of the accessible name, so any node whose name happened to contain "Iframe" could be misattached as a child iframe's tree root
- `gists.install()` didn't update the existing `_GistFinder`'s directory list on repeat calls (e.g. after `repld gist add`), leaving the finder stale while `_installed_dirs` (and therefore `_find_gist`/`_iter_gist_files`) had already moved on
- `Tab.cdp()` (the raw-CDP passthrough MCP tool) called the session directly instead of going through `_exec`, so it — unlike every other `Tab` method — couldn't recover from a stale session after an HMR reattach
- `resolve_tool()` let a broken gist's import error escape `tools/call` instead of failing soft with a warning, the way `scan_tools()` already does for `tools/list`
- `_Tee.write`'s lazy spill-file open had no lock, so two threads sharing a `task_id` (a sync cell's `asyncio.to_thread` worker racing the loop thread) could both open the spill file, leaking the first handle and losing its output; `Tab.pin()` checked-then-set `_pinned` across two `await`s, so a concurrent `pin()` call could pass the guard twice and leak a heartbeat task that later reset `_pinned=False` out from under the real one; `CDPSession.enable_fetch()`/`disable_fetch()` had the same check-await-set gap
- `browser.get(<target-id>)` raised `TabNotFoundError` when another `attach()` was already in flight for that exact target, instead of retrying like the glob-based lookup does — extracted the shared poll interval into `_ATTACH_POLL_INTERVAL_S`
- `Browser.detach_target()` let unpin/detach exceptions propagate while `detach()` swallowed them — same operation, inconsistent error handling; both now go through one `_unpin_and_detach()` helper
- `Tab.fetch()` / `browser_fetch` always read the response as `r.text()`, so binary payloads (ZIP, PDF, image, protobuf) came back as mangled mojibake instead of the actual bytes. Now reads `arrayBuffer()` and probes UTF-8 validity via `TextDecoder(..., {fatal: true})` — text responses are unchanged (JSON still auto-parsed), invalid UTF-8 is base64-encoded (native `Uint8Array.prototype.toBase64()` when available, chunked `btoa` fallback otherwise) and returned as `base64Encoded: true`, matching the `{body, base64Encoded}` shape `tab.body()` already uses
- `gists.py`'s tool-schema builder silently mapped any unrecognized parameter type (e.g. `bytes`, `Path`, `Optional[str]`) to JSON Schema `"string"` with no warning, unlike every other malformed-input path in the file; `gist_deps.py`'s `__repld_deps__` scanner had the same silent-fallback gap for non-list values

## [0.0.24] - 2026-06-24
